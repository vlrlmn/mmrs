// export class MmrsService {
//   async rateMatch(matchId: number, placements: { userId: number; place: number }[]) {
//     // TODO: calculate and update MMRs
//   }

//   async getStats(offset: number, limit: number) {
//     // const stmt = db.exec(`
//     //   SELECT matches.* FROM matches
//     //   ORDER BY date DESC
//     //   LIMIT ? OFFSET ?`);
//   }
// }

// export const mmrsService = new MmrsService();
